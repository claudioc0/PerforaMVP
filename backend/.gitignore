# Dependências do Frontend
node_modules/
.expo/

# Dependências do Backend
venv/
__pycache__/
.env

# Banco de dados local (Para não enviar os testes)
instance/
*.db

# Fotos de progresso enviadas pelos usuários (conteúdo, não código)
uploads/

# Artefatos de coverage/mutation testing (gerados localmente, ver README)
.coverage
htmlcov/
.mutmut-cache
mutmut_run*.log